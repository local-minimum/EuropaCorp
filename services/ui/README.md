# About UI

* An npm package
* One page up

## Basic structure:
- `src`
  - `index.html`
  - `img` (images)
  - `js` (scripts)
- `dist` (generate distribution from npm)

## TODO before Dockerfile ready
- `package.json`
- `httpd.conf`
